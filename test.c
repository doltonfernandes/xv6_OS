#include "types.h"
#include "user.h"

int main(int argc, char *argv[])
{
	// int pid1 = fork();
	// int pid2 = fork();
	// if(pid1 == 0 && pid2 == 0)
	// {
	// 	exit();
	// }
	while(1);
	exit();
	return 0;
}